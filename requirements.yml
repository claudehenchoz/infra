---
# Installed automatically by ansible-pull's --purge flag is unrelated;
# we install these in bootstrap.sh and via a pre-run step.
collections:
  - name: ansible.posix
    version: ">=1.5.0"
  - name: community.general
    version: ">=8.0.0"
